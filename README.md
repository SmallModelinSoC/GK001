# GK001
GK001 is a single-chip edge-AI SoC for the complete on-device voice loop: wake word, streaming ASR, local LLM, TTS. No cloud round-trip — audio never leaves the device, latency is deterministic. Linux BSP, NPU model toolchain, verified model zoo with measured memory/latency per entry. Built for engineers shipping private, offline voice products.
